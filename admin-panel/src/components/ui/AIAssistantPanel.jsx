// AIAssistantPanel removed — AI is now server-side only (api/notify.js)
// All WhatsApp notifications are sent silently via Gemini on the backend.
export default function AIAssistantPanel() {
  return null;
}
